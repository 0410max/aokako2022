<div class='recommendation-wrapper'>
    <span class='recommendation'>おすすめ</span>
    <span class='watch-all'><%= link_to 'すべて見る',end_users_path,class:'watch-all-link' %></span><br />
    <% a = 0 %>
    <% for i in -20..0 %>
        <% @users.each do |user| %>
            <% count = user.kakomons.count + user.boards.count %>
            <% if count == -i %>
                <% unless user == current_end_user %>
                    <% unless user.followed_by?(current_end_user) %>
                        <div class='board-index-recommendation-wrapper'>
                            <table>
                                <tr>    
                                    <td height='40' width='40'>
                                        <%= link_to end_user_path(user) do %>
                                            <%= image_tag user.get_profile_image ,class:'board-reccommendation-image-size'%>
                                        <% end %>
                                    </td>
                                    <td width='80' height='40'><%= link_to user.name,end_user_path(user),class:'boards-index-user-name' %></td>
                                    <td width='150' height='40' class='boards-index-td-dep'>
                                        <span class='boards-index-dep-font'><%= user.cor %></span>
                                    </td>
                                </tr>
                            </table>
                        </div>
                        <% a += 1 %>
                    <% end %>
                <% end %>
            <% end %>
            <% if a == 5 %>
                <% break %>
            <% end %>
        <% end %>
    <% end %>
</div>

